@model PayPalDemo.ViewModels.UserRoleVM

@{
    ViewData["Title"] = "Delete";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Delete</h1>

<h3>Are you sure you want to delete this?</h3>
<div>
    <h4>UserRoleVM</h4>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.RoleName)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.RoleName)
        </dd>
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Email)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Email)
        </dd>
    </dl>

    <form asp-action="Delete" method="post">
        <input type="hidden" asp-for="RoleName" value="@Model.RoleName" />
        <input type="hidden" asp-for="Email" value="@Model.Email" />
        <input type="submit" value="Delete" class="btn btn-danger" style="background-color: darkred;" /> |
        <a asp-action="Detail" asp-route-userName="@Model.Email">Back to List</a>
    </form>
</div>

